import React from 'react';
import FieldVisitForm from './FieldVisitForm';

/**
 * Rota /create-field-visit — agora renderiza o redesign executivo.
 * O motor de gravação (CreateTask) é preservado internamente.
 */
const CreateFieldVisit: React.FC = () => <FieldVisitForm />;

export default CreateFieldVisit;
